<style>
    .login-page-wrapper {
        display: flex;
        justify-content: center;
        align-items: center;
        min-height: 80vh;
        padding: 2rem;
        background: linear-gradient(135deg, #f5f7fa 0%, #c3cfe2 100%);
    }

    .login-container {
        background: white;
        padding: 3rem;
        border-radius: 12px;
        box-shadow: 0 10px 40px rgba(0, 0, 0, 0.15);
        width: 100%;
        max-width: 500px;
    }

    .login-header {
        font-family: 'Oswald', sans-serif;
        color: #044caa;
        font-size: 2rem;
        text-align: center;
        margin-bottom: 0.5rem;
    }

    .login-subtitle {
        text-align: center;
        color: #666;
        margin-bottom: 2rem;
        font-size: 0.95rem;
    }

    .form-group {
        margin-bottom: 1.5rem;
    }

    .form-group label {
        display: block;
        margin-bottom: 0.5rem;
        color: #333;
        font-weight: 600;
    }

    .form-group input {
        width: 100%;
        padding: 0.75rem;
        border: 2px solid #ddd;
        border-radius: 6px;
        font-size: 1rem;
        transition: border-color 0.3s;
    }

    .form-group input:focus {
        outline: none;
        border-color: #044caa;
        box-shadow: 0 0 0 3px rgba(4, 170, 109, 0.1);
    }

    .login-submit-btn {
        width: 100%;
        padding: 0.75rem;
        background: #044caa;
        color: white;
        border: none;
        border-radius: 6px;
        font-size: 1rem;
        font-weight: 600;
        cursor: pointer;
        transition: background 0.3s;
    }

    .login-submit-btn:hover {
        background: #055ac8;
    }

    .login-links {
        text-align: center;
        margin-top: 1.5rem;
        padding-top: 1.5rem;
        border-top: 1px solid #eee;
    }

    .login-links p {
        color: #666;
        font-size: 0.9rem;
        margin-bottom: 0.75rem;
    }

    .login-links a {
        color: #044caa;
        text-decoration: none;
        font-weight: 600;
    }

    .login-links a:hover {
        text-decoration: underline;
    }

    .error-message {
        background: #fee2e2;
        color: #991b1b;
        padding: 0.75rem;
        border-radius: 6px;
        margin-bottom: 1.5rem;
        text-align: center;
        font-size: 0.9rem;
    }

    .divider {
        text-align: center;
        margin: 1.5rem 0;
        color: #999;
        font-size: 0.9rem;
    }

    .guest-option {
        background: #f9f9f9;
        padding: 1rem;
        border-radius: 6px;
        text-align: center;
    }

    .guest-option p {
        color: #666;
        margin-bottom: 0.75rem;
        font-size: 0.9rem;
    }

    .guest-btn {
        display: inline-block;
        padding: 0.6rem 1.5rem;
        background: white;
        color: #0457c2;
        border: 2px solid #044caa;
        border-radius: 6px;
        text-decoration: none;
        font-weight: 600;
        transition: all 0.3s;
    }

    .guest-btn:hover {
        background: #5ea4ff;
    }
</style>

<div class="login-page-wrapper">
    <div class="login-container">
        <h1 class="login-header">Sign In</h1>
        <p class="login-subtitle">Welcome back to Pseudocoder</p>

        <% if flash[:alert] %>
        <div class="error-message"><%= flash[:alert] %></div>
        <% end %>

        <%= form_with url: login_path, local: true do |f| %>
        <div class="form-group">
            <label for="email">Email Address</label>
            <%= f.email_field :email, required: true, placeholder: "your@email.com" %>
        </div>

        <div class="form-group">
            <label for="password">Password</label>
            <%= f.password_field :password, required: true, placeholder: "••••••••" %>
        </div>

        <%= f.submit "Sign In", class: "login-submit-btn" %>
        <% end %>

        <div class="divider">or</div>

        <div class="guest-option">
            <p>Don't have an account yet?</p>
            <a href="<%= new_user_path %>" style="color: #044caa; text-decoration: none; font-weight: 600;">Create a new account</a>
        </div>

        <div class="login-links" style="border-top: none; margin-top: 1rem; padding-top: 0;">
            <p style="color: #999; font-size: 0.85rem;">Want to try first?</p>
            <a href="/guest_login" class="guest-btn">Continue as Guest</a>
        </div>

        <div class="login-links" style="border-top: 1px solid #ddd; margin-top: 1.5rem; padding-top: 1rem;">
            <p style="color: #999; font-size: 0.85rem;">Forgot your password?</p>
            <a href="<%= forgot_password_path %>" style="color: #044caa; text-decoration: none;">Reset Password</a>
        </div>
    </div>
</div>
</body>

</html>